# Portfolio asset placeholders

Drop your own assets into this folder without changing the React code paths unless you prefer different filenames.

Current image placeholders used by the UI:
- `your-image-here.jpg` — hero/about portrait placeholder
- `your-image-here.jpg` — project screenshot placeholder

Replace that file with your own image, or update `src/main.jsx` to use different filenames.
